package generator

import (
	"fmt"
)

func Generate(projectName, dbType string) {
	fmt.Printf("Generando proyecto '%s' con base de datos '%s'\n", projectName, dbType)
	// Aquí puedes agregar la lógica para generar el proyecto
}
